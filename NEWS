#
# some news ... see also TODO for hints
#

2016-04-08 -> ## sdk stuff
	      start with migration to my new sdk structure (like a20_sdk)

2016-04-08 -> ## toolchain stuff
	      start preparation for new architecture like a20_sdk.git

	      ## script stuff
	      some cleanups

2016-02-28 -> ## target stuff
	      update kernel to 4.4.3 and 4.4.2-rt7

	      ## toolchain stuff
	      update to 4.4.x kernel and glibc-2.23
	      update nearly all needed libs to build toolchain

2016-02-21 -> ## target stuff
	      update kernel to 4.4.2 and 4.4.1-rt6

2016-01-04 -> ## script stuff
	      add my sllin repo to external

2015-11-17 -> ## toolchain stuff
	      add project dir for led dot clock

	      ## coding stuff -> ${ARMEL_HOME}/src
	      start re-organisation of src dir
	      -> create a new lib for hardware support -> libarietta
	      -> move all used content to libarietta

2015-11-12 -> ## target stuff
	      update kernel to 4.2.6 and 4.1.12-rt13

2015-11-08 -> ## toolchain stuff
	      update to latest void-linux target image

2015-11-XX -> !!! IMPORTANT !!!
	      Complete re-organisation of naming sheme for the toolchain!
	      Move target image from slackware to void-linux
	      Stop support for other targets than void-linux

2015-09-10 -> ## toolchain stuff
	      build toolchain v2 for x86_64 (void-linux)

	      ## target stuff
	      update kernel to 4.2.0 (PREEMPT)
	      update kernel to 4.1.5-rt5 (FULL_RT-PREEMPT)

2015-06-14 -> ## toolchain stuff
	      build toolchain v1 for x86_64

	      ## target stuff
	      update kernel to 4.0.5 (PREEMPT and FULL_RT-PREEMPT)

	      ## script stuff
	      get_latest_linux_kernel.sh ... download also latest rt-patch

2015-06-08 -> !!! IMPORTANT !!!
	      Complete re-organisation of naming sheme for the toolchain!
	      The version numbers start again with version 1 but also with support for
	      armv7hl and host parts

2015-06-08 -> ## sdk stuff
	      add can-utils and libsocketcan to external
	      add support for host_* parts of the toolchain

2015-05-25 -> ## docu stuff
	      add "make docs" skeleton
	      add "make man" skeleton

2015-05-24 -> ## sdk stuff
	      add 00-INDEX as an info to all dir (where it makes sense)
	      add license info to all file
	      add include dir with a common makefile include which defines
   	      stuff like cross compiler ...

	      ## coding stuff -> ${ARMEL_HOME}/src
	      add ${ARMEL_HOME}/src/libnfc skeleton to handle nfc stuff ...
   	      needed for my clock
	      add ${ARMEL_HOME}/src/libirc skeleton for remote control handling
              (see http://www.lirc.org/) ... needed for my clock
	      add ${ARMEL_HOME}/src/libmax7219 skeleton  as a possible display for my clock
   	      (http://www.amazon.de/gp/product/B00Q6Z3Y6K?psc=1&redirect=true&ref_=oh_aui_detailpage_o00_s00)
	      add ${ARMEL_HOME}/src/libdcf77 skeleton as a clock source
	      add ${ARMEL_HOME}/src/libadc skeleton as input for small keyboard
	      add ${ARMEL_HOME}/src/libpwm skeleton to control the brightness of the max7219 "display"
	      add ${ARMEL_HOME}/src/libkeys skeleton for a simple adc based keyboard
	      add c & h skeletons to all dirs below src

	      ## coding stuff -> ${ARMEL_HOME}/template
	      add a small lib template

2015-05-23 -> ## toolchain stuff
	      update toolchain to v3 (for x86_64)
	      add buildroot config to use sysroot
	      add some default addtions to sysroot like libconfuse ...

	      ## script stuff
	      add u-boot, olimex docs and mydriver and remove old stuff

	      ## target stuff
	      add kernel config for 4.0.4 with rt1

	      ## sdk stuff
	      remove aria and rpusbdispl parts -> i have no more access to those
	      devices
	      rework ${ARMEL_HOME}/external/makefile to make individual clones
	      possible (needed on the target)


2015-05-04 -> ## target stuff
	      update toolchain to reflect slackware current updates


2015-04-13 -> ## script stuff
	      add script to clone useful external repos